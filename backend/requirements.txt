# ─── Web Framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ─── Database ─────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
pymysql==1.1.1
alembic==1.13.1

# ─── Auth & Security ──────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pydantic[email]==2.7.1
python-dotenv==1.0.1
pydantic-settings==2.2.1

# ─── MQTT ─────────────────────────────────────────────────────────────────────
paho-mqtt==1.6.1
asyncio-mqtt==0.16.2

# ─── Computer Vision & AI ─────────────────────────────────────────────────────
opencv-python==4.9.0.80
ultralytics==8.2.24
mediapipe==0.10.14
torch
torchvision

# ─── Async ────────────────────────────────────────────────────────────────────
httpx==0.27.0
websockets==12.0

# ─── Utils ────────────────────────────────────────────────────────────────────
pillow==10.3.0
numpy==1.26.4

# ─── Recommendation Engine (recommendation/ + routers/recommendations.py, ai.py) ─
# محرّك التوصيات الذكي — يحمّل نموذج تضمين نصي (embeddings) ويبحث بفهرس
# FAISS مبني مسبقاً (products.index / products.pkl) لاقتراح منتجات مشابهة
# وتصفيتها حسب حساسية/حالة العميل الصحية. اختياري بالكامل: بقية المشروع
# يعمل طبيعياً حتى لو لم تُثبَّت هذه الحزم — راجع
# routers/analysis.py::_recommendation_engine_extras() و
# recommendation/recommender.py (استيراد محمي بـ try/except).
pandas==2.2.2
faiss-cpu==1.8.0
sentence-transformers==3.0.1

