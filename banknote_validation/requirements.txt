# --- Denomination classifier (infer.py) ---
onnxruntime
opencv-python
numpy

# --- UV authenticity pipeline (alignment.py + banknote_detector.py) ---
pytesseract
# Also requires the Tesseract OCR binary itself to be installed on the OS
# (not a pip package):
#   macOS   : brew install tesseract
#   Ubuntu  : sudo apt install tesseract-ocr
#   Windows : https://github.com/UB-Mannheim/tesseract/wiki
# Then update TESSERACT_PATH at the top of banknote_detector.py to match
# where it was installed.

# --- Training only (run on PC/Colab, NOT needed to just run main.py) ---
torch>=2.0
torchvision>=0.15
onnx
onnxscript